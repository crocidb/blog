This is my blog. It's accessible through: https://crocidb.com

## Local Build

This blog uses `nix-shell` to install all dependencies for development and build.

Serve it with auto-rebuild with:

```shell
hugo -b http://localhost:1313/ server --watch
```

## Contribute

Did you find a typo and want to have it fixed? Submit a PR!
